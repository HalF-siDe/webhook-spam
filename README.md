# webhook-spam
webhook spam bookmarklet
```
javascript:(function(){var isSending=!0,waitTime=1e3,guiContainer=document.createElement('div');guiContainer.style.display='none',guiContainer.style.position='fixed',guiContainer.style.width='300px',guiContainer.style.height='300px',guiContainer.style.top='20px',guiContainer.style.right='20px',guiContainer.style.backgroundColor='#333',guiContainer.style.borderRadius='10px',guiContainer.style.boxShadow='0 0 10px rgba(0, 0, 0, 0.1)',guiContainer.style.zIndex='9999';var topBar=document.createElement('div');topBar.style.display='flex',topBar.style.flexDirection='column',topBar.style.alignItems='center',topBar.style.marginBottom='10px';var tabsBar=document.createElement('div');tabsBar.style.display='flex',tabsBar.style.justifyContent='space-around',tabsBar.style.width='100%',tabsBar.style.marginBottom='10px';var homeTab=createTab('Home');var settingsTab=createTab('Settings','Not finished');var otherTab=createTab('Other','Not finished');tabsBar.appendChild(homeTab.tab),tabsBar.appendChild(settingsTab.tab),tabsBar.appendChild(otherTab.tab);var currentTabContent=homeTab.content;function createTab(name){var tab=document.createElement('div');return tab.textContent=name,tab.style.cursor='pointer',tab.style.color='#ffffff',tab.style.fontWeight='bold',tab.style.marginTop='10px',tab.addEventListener('click',function(){toggleTab(content)}),{tab:tab,content:content}}function createInput(type,placeholder,value,inputHandler){var input=document.createElement('input');input.type=type,input.placeholder=placeholder,input.value=value||'',input.style.width='90%',input.style.boxSizing='border-box',input.style.marginBottom='10px',input.style.marginTop='10px',input.style.padding='8px',input.style.border='1px solid #ccc',input.style.borderRadius='4px',inputHandler&&input.addEventListener('input',inputHandler);return input}function createTextarea(placeholder){var textarea=document.createElement('textarea');return textarea.placeholder=placeholder,textarea.style.width='90%',textarea.style.boxSizing='border-box',textarea.style.marginBottom='10px',textarea.style.marginTop='10px',textarea.style.padding='8px',textarea.style.border='1px solid #ccc',textarea.style.borderRadius='4px',textarea.style.resize='vertical',textarea.style.height='80px',textarea}function createButton(text,clickHandler){var button=document.createElement('button');return button.textContent=text,button.style.width='90%',button.style.height='40px',button.style.cursor='pointer',button.style.border='none',button.style.borderRadius='4px',button.style.marginTop='10px',button.addEventListener('click',clickHandler),button}function updateButtonColor(){stopButton.style.backgroundColor=isSending?'red':'green'}var waitTimeInput=createInput('number','Wait time (ms)',waitTime,function(){waitTime=parseInt(waitTimeInput.value,10)||1e3});var webhookInput=createInput('text','Enter webhook URL');var messageInput=createTextarea('Enter message to send (supports ASCII art)');var stopButton=createButton('Stop Sending',toggleSending);stopButton.style.backgroundColor='red',homeTab.content.appendChild(waitTimeInput),homeTab.content.appendChild(webhookInput),homeTab.content.appendChild(messageInput),homeTab.content.appendChild(stopButton);function sendWebhookMessages(message,webhookUrl){if(isSending){var xhr=new XMLHttpRequest;xhr.open('POST',webhookUrl,!0),xhr.setRequestHeader('Content-Type','application/json'),xhr.onreadystatechange=function(){if(xhr.readyState==4)if(xhr.status==200)console.log('Webhook request successful!');else if(xhr.status!==204)console.error('Webhook request failed with status '+xhr.status),console.log(xhr.responseText),setTimeout(function(){sendWebhookMessages(message,webhookUrl)},waitTime)}}}function toggleSending(){stopButton.textContent=isSending?'Start Sending':'Stop Sending',isSending=!isSending;var enteredWebhook=webhookInput.value.trim(),enteredMessage=messageInput.value.trim();if(isSending&&enteredWebhook!==''&&enteredMessage!==''){sendWebhookMessages(enteredMessage,enteredWebhook)}updateButtonColor()}function toggleTab(content){currentTabContent.style.display='none',currentTabContent=content,currentTabContent.style.display='block'}document.body.appendChild(guiContainer),guiContainer.appendChild(topBar),topBar.appendChild(tabsBar),topBar.appendChild(homeTab.content),topBar.appendChild(settingsTab.content),topBar.appendChild(otherTab.content),document.addEventListener('keydown',function(event){'Insert'===event.code&&(guiContainer.style.display='none'===guiContainer.style.display?'block':'none')}),toggleSending()})();

```
